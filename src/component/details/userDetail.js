import React from 'react';

import { NewUser } from '../newElement/newUser';

export const UserDetail = () => {

    return(          
        <NewUser user={{title: "Usuario Seleccionado"}} />
    )
}